% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{plan_to_json}
\alias{plan_to_json}
\title{Serialize a media plan or scenario set to JSON}
\usage{
plan_to_json(x, path = NULL, pretty = TRUE)
}
\arguments{
\item{x}{A \link{MediaPlan} or \link{ScenarioSet}.}

\item{path}{Optional file path. When supplied, the JSON is written there and
\code{path} is returned invisibly; otherwise the JSON string is returned.}

\item{pretty}{Whether to pretty-print. Defaults to \code{TRUE}.}
}
\value{
A JSON string (class \code{json}), or \code{path} invisibly when writing a file.
}
\description{
Writes a \link{MediaPlan} or a \link{ScenarioSet} to JSON that \code{\link[=plan_from_json]{plan_from_json()}} can
rebuild faithfully. For a plan, only its \emph{settable} slots are written —
\verb{@data}, \verb{@grain}, \verb{@week_col}, \verb{@id}, \verb{@parent_id}, and the metadata scalars
— plus an \code{object} tag and a \code{schema_version}. Derived facts
(\code{flight_window()}, \code{pacing}, \code{week_start()}) are deliberately not stored;
they are recomputed on rebuild, so the JSON stays small and cannot persist a
stale value. A \link{ScenarioSet} writes its own scalars (\verb{@grain}, \verb{@base_name},
\verb{@id}) and each scenario serialized exactly as a standalone plan.
}
\details{
Dates are written as ISO-8601 strings, since JSON has no date type, and
missing flight cells (rows that are not part of an authored flight) are
written as \code{null}.
}
\examples{
p <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(80000, 40000)),
  grain = "channel", name = "Q3"
)
js <- plan_to_json(p)
identical(plan_from_json(js)@data, p@data)

set <- scenario_set(p)
identical(plan_from_json(plan_to_json(set))@grain, set@grain)
}
\seealso{
\code{\link[=plan_from_json]{plan_from_json()}}
}
