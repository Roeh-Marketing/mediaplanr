% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{plan_from_json}
\alias{plan_from_json}
\title{Rebuild a media plan or scenario set from JSON}
\usage{
plan_from_json(txt)
}
\arguments{
\item{txt}{A JSON string produced by \code{\link[=plan_to_json]{plan_to_json()}}, or a path to a file
containing one.}
}
\value{
A \link{MediaPlan} or a \link{ScenarioSet}, matching what was serialized.
}
\description{
Inverse of \code{\link[=plan_to_json]{plan_to_json()}}. A plan is reconstructed through
\code{\link[=media_plan_from_df]{media_plan_from_df()}}, so it is re-validated and its unit identity re-solved
on the way in: a hand-edited or drifted file rebuilds \emph{clean}, not merely
reconstructed. \verb{@id} and \verb{@parent_id} are carried through, so identity and
lineage survive.
}
\details{
Flighted and mixed plans need no special handling. The flight columns
(\code{flight_id}, \code{flight_start}, \code{flight_end}, \code{period_basis}, \code{pacing}) travel
inside \verb{@data}; their dates are reparsed and the validator accepts them.
Non-flight rows keep \code{NA} in those columns.

A \link{ScenarioSet} is detected from its \code{object} tag (or, for tagless JSON, from
the presence of \code{scenarios}) and each scenario is rebuilt the same way, with
the baseline and scenario names preserved.

Subplans are rebuilt first and then \strong{attached} through
\code{\link[=attach_subplan]{attach_subplan()}}, never assigned into the slot. Attaching recomputes the
parent's rows for each cell, so a hand-edited parent row is corrected rather
than trusted, and a file describing an illegal tree — a subplan at the
wrong grain, or a plan beneath itself — is refused with the same error an
interactive attach would give. A file nesting more than 32 levels deep is
refused outright: a written tree is always finite, a parsed one need not be.
}
\examples{
p <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(80000, 40000)),
  grain = "channel", name = "Q3"
)
plan_from_json(plan_to_json(p))
plan_from_json(plan_to_json(scenario_set(p)))
}
\seealso{
\code{\link[=plan_to_json]{plan_to_json()}}
}
