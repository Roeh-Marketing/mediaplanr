% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media-plan.R
\name{check_coverage}
\alias{check_coverage}
\title{Check a plan against a decomp (the one pairing operation)}
\usage{
check_coverage(plan, decomp, through = NULL)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}

\item{decomp}{A data frame of the decomp's line items. May name any subset of
\code{\link[=line_item_grain]{line_item_grain()}} — a channel-only decomp checks channels and ignores
partners.}

\item{through}{Optional \code{Date}. Only plan rows with \code{week < through} are
considered. Without it (or on a plan with no week column) the whole plan is
considered.}
}
\value{
Invisibly, a character vector of the decomp line items missing from
the plan — empty when coverage is complete. Returned so a UI can render
them; the warning is for interactive use.
}
\description{
A plan on its own holds intent and is blind to what has already happened.
Pair it with a decomp — which reports actualised spend and attributed KPI
through a date — and questions about history become answerable. This is the
first of those: \strong{does the plan account for everything the model measured?}
}
\details{
Every line item in \code{decomp} should appear in the plan rows dated before
\code{through}. Missing ones are reported. Line items the plan has and the decomp
does not are \emph{never} flagged: the future portion is expected to introduce new
partners and tactics, and the historical portion may carry line items the
decomp never modelled.

This \strong{warns rather than errors}. Both artifacts are individually valid; it
is their pairing that disagrees, so an app should surface the mismatch and
let the user resolve it, not refuse to load the plan.
}
\examples{
p <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(80, 40)),
  grain = "channel", name = "Q3 plan"
)
check_coverage(p, data.frame(channel = c("TV", "Search")))
}
