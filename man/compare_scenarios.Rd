% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario-set.R
\name{compare_scenarios}
\alias{compare_scenarios}
\title{Compare the scenarios in a set}
\usage{
compare_scenarios(set, level = c("summary", "cell", "flight"))
}
\arguments{
\item{set}{A \link{ScenarioSet}.}

\item{level}{One of \code{"summary"}, \code{"cell"} or \code{"flight"}.}
}
\value{
A data frame.
}
\description{
Collects the scenarios into a comparison table over \strong{planned planned_spend} at one
of two levels:
}
\details{
\itemize{
\item \code{"summary"} (default): one row per scenario — total planned_spend and the
delta versus the baseline.
\item \code{"cell"}: one row per scenario x grain cell, over the \strong{union} of grain
cells across the whole set — planned spend, that cell's share of the
scenario total, and the delta versus the same cell in the baseline.
}

The union matters when scenarios are authored independently rather than
derived — one plan per tab of a workbook, say — because their row sets need
not match. A cell absent from a scenario means it plans no spend there, so it
is zero-filled rather than omitted. Without that, a scenario that \emph{drops} a
line item would simply have no row for it and the cut would be invisible,
while one that \emph{adds} a line item would report \code{NA} instead of the full
amount. Scenarios built with \code{\link[=build_scenario]{build_scenario()}} always share their parent's
row set, so for those the result is unchanged.

Predicted outcomes are not computed here. To compare modeled volume, forecast
the scenarios with \code{mrmopt} and join the result on \code{scenario} + the grain
columns.
}
\section{Comparing flights}{

\code{level = "flight"} compares the \strong{buys} rather than the cells: one row per
scenario per flight, joined on \code{flight_id}, with what happened to it named in
a \code{change} column — \code{moved}, \code{resized}, \code{moved & resized}, \code{added},
\code{dropped}, \code{repaced} or \code{unchanged}.

This is the thing cell-level cannot say. A flight pushed a week later shows
at cell level as money leaving one week and arriving in another, which is
true but leaves the reader to infer that a single buy moved. Here it is one
row saying so.

It works by \strong{lineage}. \code{flight_id} is minted per import, so it identifies
the same buy across a plan and the scenarios derived from it, and does \emph{not}
match across independently-authored plans — which would report one buy as two.
When the set shares no flight ids at all, this warns rather than returning a
table of spurious adds and drops. A set whose plans record no flights returns
no rows, the same way \code{\link[=flights]{flights()}} does.
}

\examples{
base <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(80, 40)),
  grain = "channel", name = "base"
)
set <- scenario_set(base)
set <- add_scenario(set, build_scenario(base, edits = c("Search" = 120),
                                        name = "Search boost"))
compare_scenarios(set)
compare_scenarios(set, "cell")
}
