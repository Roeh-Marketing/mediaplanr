% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/units.R
\name{cost_per_unit}
\alias{cost_per_unit}
\alias{cpm}
\title{The cost of one unit, and the cost per thousand}
\usage{
cost_per_unit(plan)

cpm(plan)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}
}
\value{
A numeric vector, one per row of \verb{@data}. \code{NA} where a row records no
units.
}
\description{
Derived from \code{planned_spend} and \code{planned_units}, never stored separately, so
neither can disagree with the plan's money.
}
\details{
\code{cost_per_unit()} is always the price of a single unit. \code{cpm()} is the trade's
cost per thousand — the figure actually quoted for impressions — and is
simply \code{cost_per_unit() * 1000}, reported for any unit type on the assumption
that you asked for a reason.
}
\examples{
p <- media_plan_from_df(
  data.frame(channel = c("Social", "Search"),
             unit_type = c("impression", "click"),
             planned_spend = c(140000, 95000),
             planned_units = c(28000000, 118750)),
  grain = "channel", name = "Q3 plan"
)

cost_per_unit(p)
cpm(p)
p@data$planned_rate      # the CPM for Social, the CPC for Search
}
