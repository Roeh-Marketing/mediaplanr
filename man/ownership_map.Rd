% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{ownership_map}
\alias{ownership_map}
\title{Who owns each cell of a plan}
\usage{
ownership_map(plan)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}
}
\value{
A data frame with the plan's line item columns, its week column
when it has one, and:
\describe{
\item{\code{key}}{The line item, as \code{\link[=line_item]{line_item()}} names it.}
\item{\code{owner}}{The subplan key that owns the cell; \code{NA} when the plan
does.}
\item{\code{owner_name}}{That subplan's \verb{@name}; \code{NA} when the plan owns it.}
\item{\code{locked}}{\code{TRUE} when a subplan owns the cell.}
\item{\code{planned_spend}}{The cell's spend.}
}
Ordered by period, then line item.
}
\description{
One row per line item per period — the plan's own rows, labelled with the
subplan that owns each one. A cell backed by a subplan is \strong{locked} on this
plan: its number is the subplan's rollup and every edit door refuses it. The
rest are the plan's own, editable here. This is the projection behind an
ownership grid — line items across, weeks down, coloured by owner — and the
planner's answer to \emph{where can I still change things?}
}
\details{
Ownership is one level deep by construction: a cell belongs to the subplan
attached \emph{here}, whatever that subplan holds beneath it. Ask the subplan for
its own map.
}
\examples{
topline <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(100, 40)),
  grain = "channel", name = "Q3 topline"
)
tv <- media_plan_from_df(
  data.frame(channel = "TV", partner = c("NBC", "ESPN"),
             planned_spend = c(70, 50)),
  grain = c("channel", "partner"), name = "TV detail"
)
ownership_map(attach_subplan(topline, tv))
}
\seealso{
\code{\link[=subplan_map]{subplan_map()}}, \code{\link[=attach_subplan]{attach_subplan()}}
}
