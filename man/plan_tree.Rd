% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{plan_tree}
\alias{plan_tree}
\title{Print a plan's subplan tree}
\usage{
plan_tree(plan)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}
}
\value{
The printed lines, invisibly, as a character vector.
}
\description{
The console view of \code{\link[=subplan_map]{subplan_map()}}: the plan on the first line, each
subplan beneath the cell it backs, and — under any plan that holds subplans
— an \code{(editable)} line for the cells no subplan owns. It answers the
planner's question, \emph{what is locked, and who owns it?}, without leaving R.
}
\examples{
topline <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(100, 40)),
  grain = "channel", name = "Q3 topline"
)
tv <- media_plan_from_df(
  data.frame(channel = "TV", partner = c("NBC", "ESPN"),
             planned_spend = c(70, 50)),
  grain = c("channel", "partner"), name = "TV detail"
)
plan_tree(attach_subplan(topline, tv))
}
\seealso{
\code{\link[=subplan_map]{subplan_map()}}, \code{\link[=plan_mermaid]{plan_mermaid()}}
}
