% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flighting.R
\name{pacing_levels}
\alias{pacing_levels}
\title{The allowed pacing patterns}
\usage{
pacing_levels()
}
\value{
A character vector of the valid, canonical values.
}
\description{
How a flight's spend is shaped across its weeks. \code{"even"} means the weekly
figures are exactly what spreading the flight's total across its days gives.
\code{"custom"} means a planner has shaped it by hand and it no longer follows
that rule.
}
\details{
Pacing is \strong{re-derived after every edit}, so it describes the shape the rows
actually have rather than remembering whether anyone touched them. Two
consequences worth knowing:
\itemize{
\item Scaling a whole flight, or setting its total, leaves it \code{"even"} — every
week moves in proportion, so only the amount changed, not the shape.
\item Editing one week, or adding a flat amount to weeks of unequal length,
makes it \code{"custom"}.
}

Either way the flight survives: its \code{flight_id} is never dropped, so a
hand-shaped buy is still one buy and \code{\link[=flights]{flights()}} still reports it whole.
}
\examples{
pacing_levels()
}
