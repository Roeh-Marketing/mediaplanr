% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inventory.R
\name{grain_values}
\alias{grain_values}
\title{The values present on a grain column}
\usage{
grain_values(plan, col = NULL)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}

\item{col}{Name of a single grain column. \code{NULL} (the default) returns a
named list covering every grain column, including the week.}
}
\value{
Sorted unique values for \code{col}, in the column's own type; or a named
list of those vectors when \code{col} is \code{NULL}.
}
\description{
Answers "which channels are in this plan?" — for whatever the user called
their channel column. The package never privileges a column name: \code{channel}
is simply a dimension someone chose to key on, and a plan may key on
\code{media_type} or \code{vehicle} instead. So this takes the column by name and
errors, listing the alternatives, when asked for one the plan does not have.
}
\details{
The values are exactly what \code{\link[=build_scenario]{build_scenario()}}'s \code{target} accepts, so an
answer from here can be passed straight into an edit without translation.
}
\examples{
p <- media_plan_from_df(
  data.frame(channel = c("TV", "TV", "Search"),
             partner = c("NBC", "ESPN", "Google"),
             planned_spend = c(30, 20, 50)),
  grain = c("channel", "partner"), name = "Q3 plan"
)

grain_values(p, "channel")
grain_values(p)
}
