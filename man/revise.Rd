% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media-plan.R
\name{revise}
\alias{revise}
\title{Revise a plan's metadata without changing what it plans}
\usage{
revise(plan, ...)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}

\item{...}{Named metadata fields to change: \code{name}, \code{nickname},
\code{advertiser}, \code{planner}, \code{status}, \code{objective}, \code{revision}. At least one.}
}
\value{
The same plan (same \verb{@id}) with the named fields changed.
}
\description{
The metadata-only edit verb. Changes any of \code{name}, \code{nickname},
\code{advertiser}, \code{planner}, \code{status}, \code{objective} and \code{revision}, and leaves
everything else — \verb{@data}, \verb{@grain}, \verb{@id}, \verb{@parent_id}, \verb{@subplans} —
exactly as it was. Because \verb{@id} is kept, the result is \emph{the same plan},
revised, rather than a derivative of it; that is the difference from
\code{\link[=build_scenario]{build_scenario()}}, which mints a new id and lineage.
}
\details{
This exists so that a consumer never has to rebuild a plan by naming its
slots by hand — the habit that silently drops any slot added later. Use
\code{revise()} for metadata, \code{\link[=build_scenario]{build_scenario()}} for spend, and
\code{\link[=attach_subplan]{attach_subplan()}} / \code{\link[=detach_subplan]{detach_subplan()}} for structure.

\verb{@revision} is never bumped automatically. Planners say \emph{Rev 2} when they
mean it, so it is set here explicitly. It prints in the header once above 1.
}
\examples{
p <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(80, 40)),
  grain = "channel", name = "Q3 plan"
)
q <- revise(p, status = "approved", revision = 2)
identical(q@id, p@id)
q@revision
}
