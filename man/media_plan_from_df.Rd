% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media-plan.R
\name{media_plan_from_df}
\alias{media_plan_from_df}
\title{Build a validated media plan from a data frame}
\usage{
media_plan_from_df(
  df,
  grain,
  week = NULL,
  planned_spend = "planned_spend",
  planned_units = NULL,
  planned_rate = NULL,
  unit_type = NULL,
  name,
  nickname = "",
  advertiser = "",
  planner = "",
  status = "",
  objective = "",
  id = NULL,
  parent_id = character(0)
)
}
\arguments{
\item{df}{A data frame holding the plan: grain columns plus a spend column.
All other columns are preserved on \verb{@data} untouched.}

\item{grain}{Character vector naming the columns that identify a row,
including the week column when the plan is weekly.}

\item{week}{Optional name of the week column. Must be among \code{grain}. Values
are coerced to \code{Date} (the week start).}

\item{planned_spend}{Name of the planned-spend column in \code{df}. Renamed to
\code{planned_spend}. Holds intent on every row, including past weeks. Default
\code{"planned_spend"}.}

\item{planned_units, planned_rate, unit_type}{Optional names of the columns
recording what the line item buys: how much of it, at what price, and of
what (\code{"impression"}, \code{"click"}, \code{"grp"}, ...). Renamed to the canonical
names. The three quantities are bound by
\code{planned_spend = planned_units * planned_rate / rate_per(unit_type)}, where
\code{rate_per} is 1000 for impressions and 1 otherwise, so \strong{supply any two and
the third is computed} — a budget and a negotiated CPM give the
impressions, a delivery goal and a rate give the budget. See
\code{\link[=cost_per_unit]{cost_per_unit()}} and \code{\link[=unit_type_levels]{unit_type_levels()}}.}

\item{name}{Formal plan name. \strong{Required} — every plan carries one.}

\item{nickname}{Optional short working handle used in preference to \code{name}
when labelling scenarios.}

\item{advertiser}{Optional advertiser / client.}

\item{planner}{Optional person responsible.}

\item{status}{Optional workflow state; one of \code{\link[=status_levels]{status_levels()}} (matched
case-insensitively) or \code{""} when unset.}

\item{objective}{Human-facing objective / notes.}

\item{id}{Optional explicit id; generated when \code{NULL}.}

\item{parent_id}{Optional parent id for lineage.}
}
\value{
A validated \link{MediaPlan}.
}
\description{
The primary entry point for turning an uploaded plan into a \link{MediaPlan}.
Normalises the spend column name to \code{planned_spend}, coerces the week column
to \code{Date}, and runs the full validator on construction.
}
\details{
It knows nothing about decomps: a plan is constructed from the plan alone.
To check a plan against a decomp, call \code{\link[=check_coverage]{check_coverage()}} — which can then be
re-run whenever the decomp refreshes, without rebuilding the plan.
}
\examples{
df <- data.frame(
  channel = c("TV", "Search", "Social"),
  planned_spend = c(100, 80, 60)
)
media_plan_from_df(df, grain = "channel", name = "Q3 plan",
                   advertiser = "Acme", status = "in development")
}
