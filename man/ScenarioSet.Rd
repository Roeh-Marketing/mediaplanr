% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario-set.R
\name{ScenarioSet}
\alias{ScenarioSet}
\title{A base plan plus named scenarios derived from it}
\usage{
ScenarioSet(
  scenarios = list(),
  grain = character(0),
  base_name = "base",
  id = new_id("set")
)
}
\arguments{
\item{scenarios}{Named list of \link{MediaPlan} objects, all at the same grain.}

\item{grain}{Character vector; the common plan grain.}

\item{base_name}{Name of the baseline scenario (used for deltas).}

\item{id}{Opaque synthetic id.}
}
\value{
A \code{ScenarioSet} S7 object.
}
\description{
\code{ScenarioSet} is the comparison registry: a base plan and the scenarios
derived from it, all at one grain. Each entry is a \link{MediaPlan}; the set adds
names, a designated baseline, and comparison over planned_spend.
}
\details{
Build one with \code{\link[=scenario_set]{scenario_set()}} and grow it with \code{\link[=add_scenario]{add_scenario()}}; both
return new objects (value semantics — the set being compared against is never
mutated).

The set holds \strong{plan intent only}. Predicted outcomes come from a separate
modeling process (e.g. \code{mrmopt}); join them to \code{\link[=compare_scenarios]{compare_scenarios()}} output
by scenario name and grain columns.
}
