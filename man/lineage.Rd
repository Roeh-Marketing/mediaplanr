% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{lineage}
\alias{lineage}
\title{The derivation tree of a scenario set}
\usage{
lineage(set)
}
\arguments{
\item{set}{A \link{ScenarioSet}.}
}
\value{
A data frame with one row per scenario:
\describe{
\item{\code{scenario}, \code{id}, \code{parent_id}}{Its label and identity.}
\item{\code{parent}}{The label of its parent in the set; \code{NA} for a root.}
\item{\code{depth}}{0 for a root, parent's depth + 1 otherwise.}
\item{\code{status}, \code{revision}, \code{spend}}{The scenario's own.}
\item{\code{spend_vs_parent}, \code{spend_pct_vs_parent}}{Against its parent;
\code{NA} for a root.}
}
}
\description{
Each scenario records the plan it was derived from in \verb{@parent_id}; a
\link{ScenarioSet} is where enough of those plans sit together for the edges to
be drawn. One row per scenario, pointing at its parent \strong{within the set}
and carrying what changed against it. A scenario whose parent is not in the
set is a root here — lineage as far as the set records it.
}
\details{
The baseline is not privileged: \code{\link[=compare_scenarios]{compare_scenarios()}} measures every
scenario against the baseline, this measures each against the plan it
actually came from. On a set built by deriving from the base they agree; on
a chain — base, a scenario from it, a scenario from \emph{that} — they do not,
and this is the one that reads as a tree.
}
\examples{
base <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(80, 40)),
  grain = "channel", name = "base"
)
s1  <- build_scenario(base, c(Search = 60), name = "Search up")
s2  <- build_scenario(s1, c(TV = 70), name = "and TV down")
set <- add_scenario(add_scenario(scenario_set(base), s1), s2)
lineage(set)
}
\seealso{
\code{\link[=plan_mermaid]{plan_mermaid()}}, \code{\link[=compare_scenarios]{compare_scenarios()}}
}
