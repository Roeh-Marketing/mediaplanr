% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subplan.R
\name{attach_subplan}
\alias{attach_subplan}
\alias{detach_subplan}
\alias{is_topline}
\title{Attach a subplan to the cell of the plan it refines}
\usage{
attach_subplan(parent, subplan)

detach_subplan(parent, key)

is_topline(plan)
}
\arguments{
\item{parent}{The topline \link{MediaPlan}.}

\item{subplan}{The \link{MediaPlan} refining one of its cells.}

\item{key}{For \code{detach_subplan()}, the cell to release — a name from
\code{names(parent@subplans)}, e.g. \code{"TV"} or \code{"TV | NBC"}.}

\item{plan}{A \link{MediaPlan}.}
}
\value{
\code{attach_subplan()} and \code{detach_subplan()} return the parent with
its \verb{@subplans} and rows updated. \code{is_topline()} returns \code{TRUE} when the
plan holds any subplan.
}
\description{
A \strong{subplan} is a full \link{MediaPlan} owned by the team that plans one line
item's detail — the TV plan by partner and daypart, daily, beneath a
\code{channel + week} topline. Attaching it makes the parent's rows for that cell
\emph{derived from} the subplan: they are replaced by the subplan's rollup and
become read-only, so there is one number with one owner. To change TV's
total, change the TV plan and attach it again; re-attaching \strong{is} the
reconcile.
}
\section{Which cell}{

The subplan says which cell it backs through its \strong{data}, not through an
argument. Its \code{\link[=line_item_grain]{line_item_grain()}} must \emph{contain} the parent's — the same
columns or more — and across the parent's columns its rows must hold exactly
one combination: \code{channel == "TV"} on every row. That is the cell. A subplan
whose rows span two channels is refused; it would be a plan, not a
refinement.
}

\section{Cadence}{

A weekly parent needs a subplan with a week column; its spend is re-cut onto
the parent's weeks with \code{\link[=calendarize]{calendarize()}} before the rollup, so daily flights
or a different week start land correctly. A parent with no time dimension
accepts any subplan and collapses time as \code{\link[=roll_up]{roll_up()}} would.
}

\section{What attaching changes}{

Only the parent. It keeps its \verb{@id} and \verb{@revision}; its rows at the cell
are replaced — weeks the subplan does not plan disappear, weeks it adds
appear — and the subplan is held in \verb{@subplans} under the cell's key. The
subplan itself is untouched, \verb{@parent_id} included: lineage records what a
plan was \emph{derived from}, and a subplan is authored, not derived. Attaching
to a cell the parent does not yet have simply adds it.

The parent's backed rows refuse every edit — \code{\link[=build_scenario]{build_scenario()}} errors
\emph{"channel TV is planned in a subplan; edit the subplan and re-attach"} —
which means a whole-plan op like \code{list(total = 200)} errors on a topline.
Ops that avoid backed rows work, and the scenario carries the subplans.
\code{\link[=roll_up]{roll_up()}} drops them, since a rollup is a coarser view.

A subplan is a \code{MediaPlan}, so it may hold subplans of its own. A plan
cannot be attached beneath itself.
}

\examples{
topline <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(100, 40)),
  grain = "channel", name = "Q3 topline"
)
tv <- media_plan_from_df(
  data.frame(channel = "TV", partner = c("NBC", "ESPN"),
             planned_spend = c(70, 50)),
  grain = c("channel", "partner"), name = "TV detail"
)
p <- attach_subplan(topline, tv)
p@data                      # TV is now 120, from the subplan
is_topline(p)

# TV is owned by its subplan
try(build_scenario(p, list(target = list(channel = "TV"), scale = 2),
                   name = "double TV"))

detach_subplan(p, "TV")@subplans
}
