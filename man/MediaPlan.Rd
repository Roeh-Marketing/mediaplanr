% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media-plan.R
\name{MediaPlan}
\alias{MediaPlan}
\title{A media plan at a configurable grain}
\usage{
MediaPlan(
  data = data.frame(),
  grain = character(0),
  week_col = character(0),
  id = new_id("plan"),
  parent_id = character(0),
  name = "",
  nickname = "",
  advertiser = "",
  planner = "",
  status = "",
  objective = ""
)
}
\arguments{
\item{data}{A data frame with the grain columns + \code{planned_spend}. One row per grain
cell.}

\item{grain}{Character vector naming the columns that identify a row. When
the plan is weekly this includes the week column.}

\item{week_col}{Optional name of the week column within \verb{@grain}. Must hold
\code{Date} values (the week start). Empty means the plan has no time dimension.}

\item{id}{Opaque synthetic id (identity/lineage only).}

\item{parent_id}{Id of the plan this one was derived from; empty for a root
plan.}

\item{name}{Formal plan name. \strong{Required} — every plan is named.}

\item{nickname}{Optional short working handle, e.g. \code{"aggressive TV"}. Used
in preference to \verb{@name} when labelling scenarios, so a set of in-progress
scenarios reads well without renaming the formal plan.}

\item{advertiser}{Optional advertiser / client this plan belongs to.}

\item{planner}{Optional person responsible for the plan.}

\item{status}{Optional workflow state; one of \code{\link[=status_levels]{status_levels()}}, or \code{""} when
unset.}

\item{objective}{Human-facing objective / notes.}
}
\value{
A \code{MediaPlan} S7 object.
}
\description{
\code{MediaPlan} is a typed wrapper around a flat plan table. \verb{@data} is a plain,
directly accessible data frame of the grain columns plus a \code{planned_spend} column;
the class adds a validator, a synthetic identity (\verb{@id}), and lineage
(\verb{@parent_id}).
}
\details{
A plan holds \strong{planned values only} — \code{planned_spend} is what was intended
for a row, including for weeks that have already passed. It is never what
actually happened; actualised spend and attributed KPI live in the decomp.

A plan on its own is therefore \strong{blind to past and future}. "Historical",
"future", under-delivery and over-delivery are not properties of a plan: they
only exist for a \emph{pairing} of a plan with a decomp that reports actuals
through a given date. That is why no through-date is stored here, and why
editing a row for a past week is a perfectly ordinary thing to do — you are
revising what was planned, and the plan cannot know that week has passed.
See \code{\link[=check_coverage]{check_coverage()}} for the one pairing operation the package provides.

Any other columns on the supplied data frame are carried through untouched.

Construct one with \code{\link[=media_plan_from_df]{media_plan_from_df()}} rather than calling the constructor
directly.
}
\section{Derived properties}{

\verb{@flight_start}, \verb{@flight_end} and \verb{@flight_days} are computed from \verb{@data}
on every read and cannot be assigned. They describe the plan's own extent —
the dates it has line items for — and so are true of the plan alone. See
\code{\link[=flight_window]{flight_window()}} for why that is a different thing from the decomp
through-date \code{\link[=check_coverage]{check_coverage()}} takes, and why only one of the two can live
on a plan.
}

