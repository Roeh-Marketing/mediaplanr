% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{subplan_map}
\alias{subplan_map}
\title{The subplan tree as a table}
\usage{
subplan_map(plan)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}
}
\value{
A data frame with one row per plan in the tree:
\describe{
\item{\code{depth}}{0 for \code{plan}, 1 for its subplans, and so on.}
\item{\code{key}}{The parent cell this plan backs (\code{"TV"}, \code{"TV | NBC"});
\code{NA} for the root.}
\item{\code{path}}{Keys from the root joined with \code{" > "}; \code{""} for the root.}
\item{\code{name}, \code{nickname}, \code{status}, \code{revision}, \code{id}}{The plan's own.}
\item{\code{grain}}{Its grain columns joined with \code{" + "}.}
\item{\code{cadence}}{\code{"timeless"}, \code{"weekly"}, \code{"daily"}, \code{"monthly"} or
\code{"flighted"}, read off the data.}
\item{\code{line_items}, \code{rows}, \code{spend}}{Its size.}
\item{\code{subplans}}{How many it holds directly.}
}
}
\description{
One row per plan in the tree — the plan itself at depth 0, then every
subplan in pre-order, each with the cell it backs, its depth, and the facts
a viewer needs to draw or list it. This is the projection a UI builds a tree
from; \code{\link[=plan_tree]{plan_tree()}} and \code{\link[=plan_mermaid]{plan_mermaid()}} are two renderings of it.
}
\examples{
topline <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(100, 40)),
  grain = "channel", name = "Q3 topline"
)
tv <- media_plan_from_df(
  data.frame(channel = "TV", partner = c("NBC", "ESPN"),
             planned_spend = c(70, 50)),
  grain = c("channel", "partner"), name = "TV detail"
)
subplan_map(attach_subplan(topline, tv))
}
\seealso{
\code{\link[=plan_tree]{plan_tree()}}, \code{\link[=plan_mermaid]{plan_mermaid()}}, \code{\link[=attach_subplan]{attach_subplan()}}
}
