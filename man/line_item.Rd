% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media-plan.R
\name{line_item}
\alias{line_item}
\title{Line item identifier for each row}
\usage{
line_item(df, cols, sep = " | ")
}
\arguments{
\item{df}{A data frame containing the columns.}

\item{cols}{Character vector of column names to combine.}

\item{sep}{Separator between parts. Default \code{" | "}.}
}
\value{
A character vector of identifiers, length \code{nrow(df)}.
}
\description{
Collapses the given columns into a single character vector of line item
identifiers, one per row. A \strong{line item} is a specific combination of the
plan's descriptive dimensions — channel, partner, tactic — and is how the
package identifies what a row is buying. This is what the decomp constrains
and what response models attach to.
}
\details{
At a weekly grain a row is a line item \emph{for a given week}, so pass
\code{\link[=line_item_grain]{line_item_grain()}} to identify the line item independent of time, or the
full \verb{@grain} to identify the row.
}
\examples{
d <- data.frame(channel = c("TV", "TV"), partner = c("NBC", "ESPN"))
line_item(d, c("channel", "partner"))
}
