% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flight.R
\name{flight_window}
\alias{flight_window}
\title{The plan's flight window}
\usage{
flight_window(plan)
}
\arguments{
\item{plan}{A \link{MediaPlan}.}
}
\value{
A \code{Date} vector of length 2, named \code{start} and \code{end}. Length 0 when
the plan has no time dimension, no rows, or no non-missing dates — never
\code{NA}, so absence is tested with \code{length()}, exactly as for \verb{@week_col}.
}
\description{
The first and last dates on which the plan has any line item in market — the
plan's own \strong{extent}, derived from \verb{@data}. A weekly row runs from its week
start through the following six days, so a plan whose last week begins
\code{2026-04-06} is in market through \code{2026-04-12}.
}
\details{
This is a property of the plan alone: it changes only when the plan changes.
It is emphatically \strong{not} the \code{through} date of \code{\link[=check_coverage]{check_coverage()}}, which is
the boundary of what a decomp has measured. That one belongs to a \emph{pairing}
rather than to either artifact, and it moves every time the decomp refreshes.
The rule the package follows, and the reason one is a property while the
other is an argument: \emph{if a value can change without the plan changing, it
does not belong on the plan.}

\verb{@flight_start} and \verb{@flight_end} on a \link{MediaPlan} read from here.
}
\examples{
p <- media_plan_from_df(
  data.frame(week    = as.Date(c("2026-03-02", "2026-04-06")),
             channel = c("TV", "TV"),
             planned_spend = c(30, 35)),
  grain = c("channel", "week"), week = "week", name = "Q2 plan"
)

flight_window(p)   # ends 2026-04-12: the last week runs a full seven days
p@flight_start
p@flight_days
}
