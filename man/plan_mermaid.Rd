% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{plan_mermaid}
\alias{plan_mermaid}
\title{A plan's structure, or a set's lineage, as a Mermaid diagram}
\usage{
plan_mermaid(x, direction = c("TD", "LR"))
}
\arguments{
\item{x}{A \link{MediaPlan} or a \link{ScenarioSet}.}

\item{direction}{Mermaid layout direction; \code{"TD"} (default) or \code{"LR"}.}
}
\value{
A single string. \code{cat()} it, or pass it to a Mermaid renderer.
}
\description{
Emits a Mermaid \code{graph} definition as a single string. For a \link{MediaPlan}
the nodes are the plans in its subplan tree and the edges are labelled with
the cell each subplan backs; for a \link{ScenarioSet} the nodes are the
scenarios and each edge carries the spend change against the parent it was
derived from. No graphics dependency: the string renders wherever Mermaid
does — a GitHub README, Quarto, Shiny, a notebook.
}
\examples{
base <- media_plan_from_df(
  data.frame(channel = c("TV", "Search"), planned_spend = c(80, 40)),
  grain = "channel", name = "base"
)
s1  <- build_scenario(base, c(Search = 60), name = "Search up")
cat(plan_mermaid(add_scenario(scenario_set(base), s1)))
}
\seealso{
\code{\link[=subplan_map]{subplan_map()}}, \code{\link[=lineage]{lineage()}}
}
